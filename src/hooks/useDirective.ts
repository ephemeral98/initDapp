// 常用指令
export const add = (app) => {
/*   app.directive('add', {
    mounted(el, binding, vnode) {
      el.addEventListener('click', () => {
        console.log('binding...', binding);
        if(binding.value)
      });
    },
  }); */
};

/* export const sub = (app) => {
  app.directive('clickOnce', {
    mounted(el, binding, vnode) {
      el.addEventListener('click', () => {
        console.log('hello...');
      });
    },
  });
};
 */
