/**
 * full：整体替换
 * @param param
 * @returns
 */
export function $f(param: string): string {
  return param;
}

/**
 * 局部模板替换
 * @param param
 * @returns
 */
export function $tc(param: string): string {
  return param;
}
